import { LocaleCode } from '../enum/locale.enum';
import { LocaleLabelByCode } from '../interfaces/locale.interfaces';

export const localeLabelByCode: LocaleLabelByCode = {
  [LocaleCode.EN]: 'English',
  [LocaleCode.ES]: 'Española',
};
